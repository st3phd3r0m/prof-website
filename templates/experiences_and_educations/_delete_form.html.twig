<form method="post" action="{{ path('experiences_and_educations_delete', {'id': experiences_and_education.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ experiences_and_education.id) }}">
    <button class="btn">Delete</button>
</form>
